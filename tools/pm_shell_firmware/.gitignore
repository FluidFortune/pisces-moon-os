# pm_shell — local artifacts not for git
#
# Lua sources downloaded by fetch_lua.sh — versioned upstream, not here
lib/lua/*.c
lib/lua/*.h

# Tarball from fetch_lua.sh
lua-*.tar.gz

# PlatformIO build outputs
.pio/
.pioenvs/
.piolibdeps/
*.pio

# Editor cruft
.vscode/
*.swp
*.swo
.DS_Store
